using StarterAssets;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Attack : ThirdPersonController
{/*
    // agregados al script del controller para manejar el sistema de ataque

    public GameObject arrowObject;
    public Transform arrowPoint;
    public float arrowSpeed = 25f;

    private void AttackBowAndArrow()
    {
        GameObject arrow = Instantiate(arrowObject, arrowPoint.position, transform.rotation);
        arrow.GetComponent<Rigidbody>().AddForce(transform.forward * arrowSpeed, ForceMode.Impulse);
    }
    */
}
